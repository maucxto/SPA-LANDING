const slides=[...document.querySelectorAll('.hero__slide')];const dots=[...document.querySelectorAll('.hero__status span')];let current=0;if(!matchMedia('(prefers-reduced-motion: reduce)').matches)setInterval(()=>{slides[current].classList.remove('is-active');dots[current].classList.remove('is-active');current=(current+1)%slides.length;slides[current].classList.add('is-active');dots[current].classList.add('is-active')},5500);

const testimonialCards=[...document.querySelectorAll('.testimonial-card')];const testimonialDots=[...document.querySelectorAll('.testimonial-dots button')];let testimonialCurrent=0;function showTestimonial(index){testimonialCards[testimonialCurrent]?.classList.remove('is-active');testimonialCards[testimonialCurrent]?.setAttribute('aria-hidden','true');testimonialDots[testimonialCurrent]?.classList.remove('is-active');testimonialCurrent=index;testimonialCards[testimonialCurrent]?.classList.add('is-active');testimonialCards[testimonialCurrent]?.removeAttribute('aria-hidden');testimonialDots[testimonialCurrent]?.classList.add('is-active')}testimonialDots.forEach((dot,index)=>dot.addEventListener('click',()=>showTestimonial(index)));if(testimonialCards.length>1&&!matchMedia('(prefers-reduced-motion: reduce)').matches)setInterval(()=>showTestimonial((testimonialCurrent+1)%testimonialCards.length),6500);
