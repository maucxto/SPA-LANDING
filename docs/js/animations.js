const intro=document.getElementById('intro');const reduce=matchMedia('(prefers-reduced-motion: reduce)').matches;if(reduce||sessionStorage.getItem('spa-antoniros-intro'))intro.classList.add('is-done');else setTimeout(()=>{intro.classList.add('is-done');sessionStorage.setItem('spa-antoniros-intro','seen')},3600);const observer=new IntersectionObserver(entries=>entries.forEach(entry=>{if(entry.isIntersecting){entry.target.classList.add('is-visible');observer.unobserve(entry.target)}}),{threshold:.14});document.querySelectorAll('.reveal').forEach(el=>observer.observe(el));
